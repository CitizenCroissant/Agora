# Mobile App Environment Variables
# Copy this file to .env and update with your values (or use .env.local)

# When running in a devcontainer with LAN mode, set your HOST machine's LAN IP
# so the Expo QR code uses it instead of the container IP (required for Expo Go).
# Find your IP: VS Code Ports panel, or on host run ipconfig / ifconfig / ip addr
REACT_NATIVE_PACKAGER_HOSTNAME=192.168.1.100

# API URL for development (your machine's LAN IP) - can also be set in app.json extra.apiUrl
# Example: http://192.168.1.100:3001/api
# EXPO_PUBLIC_API_URL=http://YOUR_LAN_IP:3001/api

# Note: For production builds, set apiUrl in app.json extra.apiUrl instead
