{
  "buildCommand": "npm run build -- --filter=@agora/shared --filter=@agora/api",
  "outputDirectory": ".",
  "functions": {
    "api/**/*.ts": {
      "memory": 1024,
      "maxDuration": 10
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
